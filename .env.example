# Database (PostgreSQL)
# For Railway: Use Railway's PostgreSQL service or external database (e.g., Neon)
# Railway will automatically provide DATABASE_URL if you add a PostgreSQL service
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# NextAuth
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
# For Railway: Railway will set this automatically, or use your Railway app URL
# Format: https://your-app-name.up.railway.app
NEXTAUTH_URL="http://localhost:3000"

# Railway automatically sets PORT environment variable
# Next.js will use it automatically, no configuration needed
